export const metadata = {
    title: 'Contact Us',
    alternates: { canonical: '/contact' },
    description: 'Get in touch with Axelis Overseas expert counsellors today. Start your journey towards a global education with zero fees.',
};

export default function ContactLayout({ children }) {
    return <>{children}</>;
}
