export const metadata = {
  title: 'Certifications & Accreditations',
  description:
    'Axelis Overseas Education is DPIIT Startup India recognised, with British Council and AIRC counsellor training behind our study-abroad guidance.',
  alternates: { canonical: '/certifications' },
};

export default function CertificationsLayout({ children }) {
  return <>{children}</>;
}
