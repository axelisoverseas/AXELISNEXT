export const metadata = {
    title: 'About Us',
    description: 'Learn how Axelis Overseas transforms international education with transparent pricing, guaranteed admissions, and 100% Visa Success rate.',
};

export default function AboutLayout({ children }) {
    return <>{children}</>;
}
